/**
 * Canonical production origin — single source shared by `layout.tsx`
 * (`metadataBase`), `sitemap.ts` and `robots.ts` so they can't drift apart.
 */
export const SITE_URL = 'https://maps.weourus.xyz';
